###
# gcloud
###

# can't login with docker + gcloud (needs browser
#alias gcloud='docker run --rm -it -e CLOUDSDK_CONFIG=/config/mygcloud -v ~/.config/gcloud:/root/.config/gcloud -v `pwd`/mygcloud:/config/mygcloud -v `pwd`:/certs  google/cloud-sdk:alpine /bin/bash'

